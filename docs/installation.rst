============
Installation
============

At the command line::

    $ easy_install django-model-events

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-model-events
    $ pip install django-model-events
